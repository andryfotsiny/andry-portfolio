name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 8 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping DB via API
        run: |
          curl -f https://andry-portfolio-21qa5ewsd-andrys-projects-0057aa16.vercel.app/api/keepalive \
            || echo "Ping failed"
